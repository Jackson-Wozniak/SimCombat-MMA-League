package internal.mma_league.league.repository;

import internal.mma_league.league.entity.SimCombat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SimCombatRepository extends JpaRepository<SimCombat, Integer> {
}
